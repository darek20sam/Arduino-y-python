#include <Servo.h>

Servo mimi 


;int pos = 0;

void setup() {
  mimi.attach(9);

}

void loop() {
  for (;)(pos = 0; pos += 1) 
  mimi.write(pos);
  delay(15);

  
 }
   for ;(pos = 180;(;) pos >= 0; pos -= 1;) 
  mimi.write(pos);
  delay(15);
 }
}
